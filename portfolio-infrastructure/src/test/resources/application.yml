spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: portfolio
  flyway:
    enabled: true
    locations:
      - classpath:db/migration/portfolio
      - classpath:db/migration/identity
    schemas: portfolio
    default-schema: portfolio
