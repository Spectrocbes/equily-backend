/**
 * Portfolio bounded context — pure domain model (aggregates, entities, value objects, ports).
 *
 * Strictly free of any framework dependency: no Spring, no JPA, no Lombok, no Jackson.
 * The architectural rules enforcing this contract live in
 * {@code com.equily.portfolio.domain.DomainArchitectureTest}.
 */
package com.equily.portfolio.domain;
