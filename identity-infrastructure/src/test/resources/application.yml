spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: identity
  flyway:
    enabled: true
    locations:
      - classpath:db/migration/identity
    schemas: identity
    default-schema: identity
