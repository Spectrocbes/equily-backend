name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-analyze:
    name: Build, Test & Analyze
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Required for SonarCloud blame information

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Verify environment
        run: |
          echo "JAVA_HOME: $JAVA_HOME"
          java -version
          ./mvnw --version
          echo "Trusted certs in cacerts:"
          keytool -list -cacerts -storepass changeit 2>/dev/null | grep -c "trustedCertEntry"

      - name: Cache SonarCloud packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Spotless check
        run: ./mvnw spotless:check --no-transfer-progress

      - name: Build, test & coverage
        run: ./mvnw clean verify --no-transfer-progress

      - name: SonarCloud analysis
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          ./mvnw sonar:sonar \
            --no-transfer-progress \
            -Dsonar.organization=Spectrocbes \
            -Dsonar.projectKey=Spectrocbes_equily-backend \
            -Dsonar.host.url=https://sonarcloud.io